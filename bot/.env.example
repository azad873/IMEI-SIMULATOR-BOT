# Telegram bot token (from BotFather)
BOT_TOKEN=${BOT_TOKEN}

# PostgreSQL DSN (asyncpg)
# Note sslmode=require (recommended in production)
POSTGRES_DSN=${POSTGRES_DSN}

# Redis URL
REDIS_URL=${REDIS_URL}

# Comma-separated admin Telegram user IDs
ADMIN_IDS=${ADMIN_IDS}

# Secret key for HMAC-based fake location generation
SECRET_KEY=${SECRET_KEY}

# Geoapify static maps API key
GEOAPIFY_API_KEY=${GEOAPIFY_API_KEY}

# Optional webhook URL (if set, bot uses webhook mode)
WEBHOOK_URL=

# Webhook port (for Fly.io or other PaaS)
PORT=8080

# Fallback locale (en, es, ru, hi)
LOCALE_FALLBACK=en
